import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#e76f51',
      light: '#f4a261',
    },
    secondary: {
      main: '#2a9d8f',
    },
  },
});

export default theme;
